      }

      const ai = new GoogleGenAI({ apiKey });

      // Convert multer buffer to base64
      const base64Image = file.buffer.toString('base64');
      const mimeType = file.mimetype;

      const response = await ai.models.generateContent({
        model: apiModel,
        contents: [
          {
            role: 'user',
            parts: [
              {
                text: `You're a practical college student who just wants to pass, grading a peer's homework. Be extremely plain-spoken, direct, and highly concise to save tokens.

=== RUBRIC ===
${resources}
=============

Evaluate the image:
1. Does it hit the core rubric points?
2. If it's correct but super short, pass it.
3. If it's totally wrong or blank, fail it.

Output ONLY JSON:
{
  "passed": boolean,
  "feedback": "Plain-spoken feedback, 1-2 short sentences.",
  "transcribedText": "Quick transcription of the image"
}
`,
              },
              {
                inlineData: {
                  mimeType,
                  data: base64Image,
                },
              },
            ],
